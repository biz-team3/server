server:
  port: ${SERVER_PORT:8080}

spring:
  profiles:
    default: local
  config:
    import: optional:file:.env[.properties]
  application:
    name: biz-team3-server

logging:
  level:
    com.bizteam3.server: debug
