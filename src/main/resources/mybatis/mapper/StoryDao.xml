<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bizteam3.server.story.dao.StoryDao">

    <insert id="insert"
            parameterType="com.bizteam3.server.story.entity.Story"
            useGeneratedKeys="true"
            keyProperty="storyId"
            keyColumn="story_id">
        INSERT INTO stories (
        user_id,
        image_url,
        created_at,
        expires_at,
        deleted_at
        ) VALUES (
        #{userId},
        #{imageUrl},
        SYSTIMESTAMP,
        SYSTIMESTAMP + INTERVAL '24' HOUR,
        NULL
        )
    </insert>

    <select id="selectByUserIdAndStoryId" resultType="com.bizteam3.server.story.entity.Story">
        SELECT
            STORY_ID,
            USER_ID,
            IMAGE_URL,
            CREATED_AT,
            EXPIRES_AT,
            DELETED_AT
        FROM STORIES
        WHERE USER_ID = #{userId}
            AND STORY_ID = #{storyId}
            AND DELETED_AT IS NULL
            AND EXPIRES_AT > SYSTIMESTAMP
    </select>

    <select id="selectByUserId" resultType="com.bizteam3.server.story.entity.Story">
        SELECT
        story_id AS storyId,
        user_id AS userId,
        image_url AS imageUrl,
        created_at AS createdAt
        FROM STORIES
        WHERE user_id = #{userId}
        AND DELETED_AT IS NULL
        ORDER BY created_at DESC
    </select>

    <update id="delete">
        UPDATE STORIES
        SET
        DELETED_AT = SYSTIMESTAMP
        WHERE USER_ID = #{userId}
        AND STORY_ID = #{storyId}
        AND DELETED_AT IS NULL
    </update>

</mapper>
