<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bizteam3.server.post.dao.PostDao">
    <insert id="insert"
            parameterType="com.bizteam3.server.post.entity.Post"
            useGeneratedKeys="true"
            keyProperty="postId"
            keyColumn="post_id">
        INSERT INTO posts (
            user_id,
            caption,
            translated_caption,
            created_at,
            update_at
        ) VALUES (
            #{userId},
            #{caption, jdbcType=CLOB},
            #{translatedCaption, jdbcType=CLOB},
            SYSTIMESTAMP,
            SYSTIMESTAMP
        )
    </insert>

    <select id="selectUserId"
            parameterType="int"
            resultType="int">
        SELECT user_id
        FROM posts
        WHERE post_id = #{postId}
        AND delete_at IS NULL
    </select>

    <update id="updateCaption"
            parameterType="com.bizteam3.server.post.entity.Post">
        UPDATE posts
        SET caption = #{caption, jdbcType=CLOB},
        translated_caption = #{translatedCaption, jdbcType=CLOB},
        update_at = SYSTIMESTAMP
        WHERE post_id = #{postId}
        AND user_id = #{userId}
        AND delete_at IS NULL
    </update>


    <delete id="deleteMediasByPostId" parameterType="int">
        DELETE FROM medias
        WHERE post_id = #{postId}
    </delete>

    <delete id="deletePostHashtagsByPostId" parameterType="int">
        DELETE FROM post_hashtags
        WHERE post_id = #{postId}
    </delete>

    <delete id="delete" parameterType="int">
        DELETE FROM posts
        WHERE post_id = #{postId}
    </delete>

    <select id="countByPostId" parameterType="int" resultType="int">
        SELECT COUNT(*)
        FROM posts
        WHERE post_id = #{postId}
        AND delete_at IS NULL
    </select>
</mapper>
