<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bizteam3.server.post.dao.MediaDao">
    <insert id="insert"
            parameterType="com.bizteam3.server.post.entity.Media">
        INSERT INTO Media (
            post_id,
            media_type,
            media_url,
            sort_order,
            original_file_name,
            content_type,
            file_size,
            created_at
        ) VALUES (
            #{postId},
            #{mediaType},
            #{mediaUrl},
            #{sortOrder},
            #{originalFileName},
            #{contentType},
            #{fileSize},
            SYSTIMESTAMP
        )
    </insert>
</mapper>
