<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bizteam3.server.user.dao.UserDao">

    <!-- 회원가입 -->
    <insert id="insert" parameterType="com.bizteam3.server.user.entity.User">
        INSERT INTO users
            (password, name, bio, website, profile_img, account_vis)
        VALUES
            (#{password}, #{name}, #{bio}, #{website}, #{profileImg}, #{accountVis})
    </insert>

</mapper>