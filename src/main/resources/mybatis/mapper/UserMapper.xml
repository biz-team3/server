<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bizteam3.server.user.dao.UserDao">

    <!-- 회원가입 -->
    <insert id="insert" parameterType="com.bizteam3.server.user.entity.User">
        INSERT INTO users
            (username, password, name, bio, website, profile_img, account_vis)
        VALUES
            (#{username}, #{password}, #{name}, #{bio}, #{website}, #{profileImg}, #{accountVis})
    </insert>

    <select id="findAll" resultType="com.bizteam3.server.user.entity.User">
        SELECT
            user_id,
            username,
            password,
            name,
            bio,
            website,
            profile_img,
            account_vis,
            created_at,
            update_at,
            delete_at
        FROM users
        WHERE delete_at IS NULL
        <if test="query != null and query != ''">
            AND (
                LOWER(username) LIKE '%' || LOWER(#{query}) || '%'
                OR LOWER(name) LIKE '%' || LOWER(#{query}) || '%'
            )
        </if>
        ORDER BY user_id DESC
        OFFSET #{offset} ROWS FETCH NEXT #{size} ROWS ONLY
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*)
        FROM users
        WHERE delete_at IS NULL
        <if test="query != null and query != ''">
            AND (
                LOWER(username) LIKE '%' || LOWER(#{query}) || '%'
                OR LOWER(name) LIKE '%' || LOWER(#{query}) || '%'
            )
        </if>
    </select>

    <select id="findById" resultType="com.bizteam3.server.user.entity.User">
        SELECT
            user_id,
            username,
            password,
            name,
            bio,
            website,
            profile_img,
            account_vis,
            created_at,
            update_at,
            delete_at
        FROM users
        WHERE user_id = #{userId}
          AND delete_at IS NULL
    </select>

    <select id="findByUsername" resultType="com.bizteam3.server.user.entity.User">
        SELECT
            user_id,
            username,
            password,
            name,
            bio,
            website,
            profile_img,
            account_vis,
            created_at,
            update_at,
            delete_at
        FROM users
        WHERE username = #{username}
          AND delete_at IS NULL
    </select>

    <update id="update" parameterType="com.bizteam3.server.user.entity.User">
        UPDATE users
        SET
            username = #{username},
            name = #{name},
            bio = #{bio},
            website = #{website},
            profile_img = #{profileImg},
            account_vis = #{accountVis},
            update_at = SYSTIMESTAMP
        WHERE user_id = #{userId}
          AND delete_at IS NULL
    </update>

    <update id="softDelete">
        UPDATE users
        SET
            delete_at = SYSTIMESTAMP,
            update_at = SYSTIMESTAMP
        WHERE user_id = #{userId}
          AND delete_at IS NULL
    </update>

</mapper>
